# DEPRECATED — replaced by android.yml
# This file is kept only to avoid a broken reference in git history.
# It will never run automatically. Use android.yml for all Android builds.

name: Build Android APK (DEPRECATED)

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated workflow
        run: |
          echo "This workflow is deprecated."
          echo "Use the 'Build Android' workflow (android.yml) instead."
          exit 1
