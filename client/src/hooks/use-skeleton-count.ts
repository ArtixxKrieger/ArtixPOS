export function useViewportSkeletonCount(
  _itemHeight: number,
  _headerOffset = 200,
  _minCount = 2,
): number {
  return 0;
}

export function useGridSkeletonCount(
  _breakpoints: Record<number, number>,
  _rowHeight: number,
  _headerOffset = 200,
): number {
  return 0;
}
