node_modules
dist
.DS_Store
.env
server/public
vite.config.ts.*
*.tar.gz
local.db
*.db
.local/
attached_assets/
yarn.lock

# Native platform dirs — generated fresh in CI via npx cap add android/ios
ios/
android/

# Android signing keystores — NEVER commit these
keystore/*.jks
keystore/*.keystore
